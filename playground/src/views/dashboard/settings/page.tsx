import { PageShell } from '../../../ui'

export default function DashboardSettings() {
  return (
    <PageShell title="设置 /dashboard/settings" accent="#0891b2">
      <p>静态子路由，嵌套在 <code>/dashboard</code> 的 <code>&lt;Outlet/&gt;</code> 中渲染。</p>
    </PageShell>
  )
}
