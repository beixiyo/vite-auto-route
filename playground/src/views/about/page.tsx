import { PageShell } from '../../ui'

export default function About() {
  return (
    <PageShell title="关于 /about" accent="#0ea5e9">
      <p>一个最普通的静态路由，由 <code>src/views/about/page.tsx</code> 自动生成为 <code>/about</code>。</p>
    </PageShell>
  )
}
