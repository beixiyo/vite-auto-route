# 更新日志

`@jl-org/vite-auto-route` 的所有重要变更都会记录在本文件

[中文 README](./README.md) | [English README](./README.en.md)

## [2.0.2] - 2026-06-26

自动路由生成的一组缺陷修复，无公共 API 变更

### 修复

- **同级排序改为按段类型**（静态 < 动态 < 可选 < catchAll），避免静态路由在消费方「首个命中」匹配下被动态段遮蔽（如 `/posts/new` 被 `/posts/:id` 吃掉、不可达）
- **catch-all `[...slug]` 现产出 `/**`**（而非 `/:slug/*`），消费方可正确匹配任意层级路径，`path` 与 `segments` 统一为 `**` 词元，参数经 `params.splat` 读取完整剩余路径
- **根 `page.tsx` + 顶层 `[id]` 现提升为兄弟**（与非 root 路径行为对称），不再强制根组件充当布局
- **非法路由参数名**（如 `[2fa]`、`[a.b]`）改为构建期报错 fail-fast，避免运行时 path-to-regexp 匹配崩溃
- **配置合并逐字段兜底**，显式传入 `undefined` 不再冲掉默认值

### 变更

- 移除从不被读取的内部 `lastStaticParent` 逻辑；`customizeRoute` 返回类型收紧为 `FileSystemRoute`
- 新增 vitest 单测与可视化 playground

### 说明

- catch-all 的路径形态与同级排序顺序较旧版本不同，属修复既有缺陷导致的行为变更；若此前依赖旧行为，升级后请复核现有 catch-all 与同级路由的匹配

## [2.0.1] - 2025-11-16

### 修复

- 修复多级参数嵌套时路由结构生成错误

### 新增

- 新增 `customizeRoute` 配置，可自定义生成的路由结构

## [2.0.0] - 2025-11-14

### 变更

- 规范化路由参数，并支持捕获多级参数（参数形态调整，破坏性变更）

## [1.0.6] - 2025-10-10

### 修复

- 修复 Mono-repo 下扫描外部包可能导致的递归问题

## 更早版本

- 支持自定义配置，可集成任意框架（Vue / React 等）
